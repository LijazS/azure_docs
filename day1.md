# Day 1: Terraform Basics

## Core Terraform Commands

| Command | Purpose |
|---------|----------|
| `terraform init` | Initialize working directory with Terraform configuration |
| `terraform validate` | Validate Terraform configuration files |
| `terraform plan` | Preview changes before applying |
| `terraform apply` | Apply the Terraform configuration |
| `terraform destroy` | Destroy all managed infrastructure |